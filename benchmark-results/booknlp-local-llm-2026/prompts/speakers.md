# speakers v1
For each direct quotation, identify the speaker if the passage supports one. A speaker is the character speaking the quoted words, not the addressee or narrator unless narrator speaks directly. Use the speaker's mention token span from the passage when available; otherwise use null. Return only JSON matching {"speakers":[{"quote_start_token":integer,"quote_end_token":integer,"speaker_start_token":integer|null,"speaker_end_token":integer|null,"speaker_cluster":"c1"|null}]}. Use zero-based inclusive indices and consistent cluster strings for the same speaker. No explanation or extra keys.
